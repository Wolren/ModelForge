"""Model-graph layout: auto-position nodes in a Processing model.

This package owns the *node graph layout* algorithm (Sugiyama,
topological sort, etc.). It is distinct from the print-layout
and symbology pipeline, which lives in
:mod:`model_forge.compiler_core.core.services.map_builder`.
"""
